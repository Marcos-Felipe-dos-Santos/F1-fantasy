# PROGRESS — F1 Fantasy

> Estado do build por PR, pra retomar em sessão nova sem contexto anterior.
> Plano de referência: `PLANO_CLAUDE_CODE.md` §5. Regras: `CLAUDE.md`. Nada foi pushado — tudo local.

## Concluídos (mergeados na main)

- **PR 0.1–0.3** — Scaffold Vite+React+TS+Vitest, RNG semeado (mulberry32 + `deriveSeed` por rótulo), tipos base alinhados ao GDD v1.1.
- **PR 1.1** — Dataset semente (`src/data/`: 22 equipe/anos, 24 peças, 10 pistas) + loader puro validado (`src/engine/dataset.ts`).
- **PR 1.2** — Draft (`src/engine/draft.ts`): reducer puro de 5 sorteios de equipe/ano + rodada 6 de peça com escassez (2 cópias) + bots por seed (`bots.ts`).
- **PR 1.3** — Quali (`src/engine/quali.ts` + `carro.ts`): `resolverCarro` (Loadout → notas efetivas com bônus de peça, sem clamp) e `simularQuali` (score 0.5·QUALI + 0.4·carro ponderado pela pista + 0.1·CALL, variância semeada por sub-stream `quali:${jogadorId}`, desempate por jogadorId). Seed de ouro congelada (seed 42, Monza). Revisado pelo senior-reviewer (aprovado; aviso das listas de chaves paralelas corrigido com checagem `in`).

- **PR 1.4** — Corrida (`src/engine/corrida.ts`): simulação por carro independente, tempo por volta (0.5·RIT + 0.4·carro ponderado + 0.1·CALL, variância por sub-stream `corrida:${jogadorId}`), degradação por DESGASTE da pista atenuada por PNEU, pit obrigatório com janela por CALL + paradas extras por limiar de desgaste, custo de pit por PIT_TEMPO/PIT_ERRO, volta 1 com offset de grid (por dificuldade de ultrapassagem) + penalidade de LARG, pontuação FIA + volta mais rápida do grid inteiro (+1, desempate por posição final). Campo `paradas` adicionado a `ResultadoCorrida.classificacao`. Seed de ouro congelada (seed 42, Monza). Sem incidentes/DNF/clima (PR 1.5). Revisado pelo senior-reviewer (aprovado sem bloqueantes).

- **PR 1.5a** — Incidentes (`corrida.ts`): erro de piloto (CONS, perda de tempo), quebra de chassi/motor (CONF/CONF_MOTOR ⇒ DNF), risco técnico da peça (rolagem única por corrida: problema técnico numa volta + investigação com penalidade em ms só pra quem termina, GDD §8), registro de `eventos` ordenado (volta, jogadorId, tipo) pra narração. DNF: 0 pontos, classificado após os que terminaram (voltasCompletadas desc); volta mais rápida só entre quem terminou. Tipos novos: `TipoEvento`, `EventoCorrida`, `status`/`voltasCompletadas` na classificação. Ordem de consumo do RNG documentada como contrato no cabeçalho de `corrida.ts`. Revisado (aprovado; sort de eventos ganhou desempate terciário por tipo e o doc do contrato de RNG foi completado).

- **PR 1.5b** — Clima (`corrida.ts`): rolagem global de chuva 1×/corrida em sub-stream próprio (`corrida:clima`), sem consumo extra nos streams por carro; chuva soma lentidão global (`chuvaLentidao`·tempoBase) + penalidade por CHU baixo por volta e multiplica a chance de erro (`chuvaMultErro`, cap 1). Chuva NÃO altera degradação/pit/quebras/risco (pneu de chuva não modelado); quali segue seca. `chuva: boolean` em `ResultadoCorrida`. Seed de ouro seca inalterada bit a bit; segunda seed de ouro molhada (Interlagos chanceChuva:1, seed 42). Contrato de RNG documenta que o stream molhado diverge do seco a partir do 1º erro-só-na-chuva (custo consome 1 next extra). Revisado (aprovado; caveats replicados nos testes de paradas).

- **PR 1.6** — Balance-harness (`scripts/balance.ts` + `scripts/balance.balance.test.ts`, comando `npm run balance`, config própria `vitest.balance.config.ts` — fora do `npm test`) e calibração das 3 metas do dev (ver "Metas de calibração" abaixo, agora marcadas como cumpridas). Harness mede, com o dataset real: `medirVitoriaPole` (taxa de vitória do pole com carros idênticos, Monza/Spa/Mônaco), `medirParadasExtras` (fração de 2+ paradas por nível de desgaste e por bucket de PNEU, Monza/Spa/Suzuka) e `medirRaridadePeca` (championShare vs. playerShare por raridade de peça, sobre 200 campeonatos de 22 jogadores × 10 pistas com draft simplificado semeado). Baseline vermelho confirmado antes de calibrar (media grid 55.0%, alto paradas 3.6%); metas 1 e 2 calibradas mexendo só em `CORRIDA_CONFIG.variancia` (0.006→0.004), `gridOffsetMs` (facil/media/dificil 150/300/500→500/800/1200) e `limiarPneuGasto` (6→3.5); meta 3 (guarda anti-dominância da peça proibida) já passava no baseline e segue passando (ratio final 1.25, bem abaixo do limite 3.0). `npm run balance` roda em ~0.5s. Recalibração deslocou as 2 seeds de ouro de `corrida.test.ts` (recongeladas) e fortaleceu 2 testes direcionais que eram só qualitativos (limiares reais com margem de segurança abaixo do observado); os testes de clima com caveat de seed não quebraram. Revisado pelo senior-reviewer (aprovado; determinismo, fronteira engine/scripts, Ns e números do relatório verificados de forma independente).

- **PR 1.6.1** — `medirRaridadePeca` do harness trocou o draft uniforme simplificado pelo motor de draft real (`criarDraft`+`resolverBots`+`atribuirPerfis`, 22 bots, dificuldade 'dificil'). Só `scripts/balance.ts`; engine intocada. Ratio Proibida 1.25→1.51 (guarda ≤3.0 segue verde).

- **PR 1.7a** — UI do draft do modo Single (`src/ui/`): TelaInicio (seed + dificuldade) → TelaDraft (rodadas 1-5, cards dos 5 componentes com notas, Modo Craque) → TelaPeca (5 reveladas com raridade 🟢🔵🟣🟡☠️) → TelaResumo (carro final com notas base→efetivas via `resolverCarro` + grid dos 22). Transições puras em `fluxo-draft.ts` (testadas sem DOM); hook `useDraft` é casca fina; engine intocada; zero dependência nova (logo sem testes de componente DOM — instalar jsdom/testing-library exigiria aprovação do dev). Revisado (aprovado; testes de seedDeTexto e dificuldade adicionados por aviso da revisão). Rodar: `npm run dev`.

- **PR 1.7b** — UI da corrida do modo Single (fecha a Fase 1). Engine: `ResultadoCorrida.historicoVoltas: Record<string, number[]>` (tempo de cada volta completada por carro, incluindo a volta do DNF; penalidade de investigação fica fora — é pós-corrida), registro puro sem nenhuma chamada nova de RNG — as 2 seeds de ouro seguem bit a bit (testes de ouro só desestruturam o campo novo antes do `toEqual` + asserção soma≈tempoTotal). UI: `fluxo-corrida.ts` puro (`prepararCorrida` = `simularQuali`+`simularCorrida` com a seed do draft, pista fixa Monza `PISTA_CORRIDA_ID`; matemática de replay testável sem DOM: `acumularVoltas`, `progressoNoReplay`, `voltaAtual`, `fracaoVisual` — N voltas visuais, terminou congela na chegada, DNF congela em 0.5 do traçado —, `pontoNoTracado` por polilinha fechada sem `getPointAtLength`, `escalaReplay` ~2.2s de relógio por volta, `TRACADO_MONZA`); `useCorrida` (rAF com tempo em ref — agendamento fora de updater de setState, seguro sob StrictMode), `TelaCorrida` (grid da quali → Largar → SVG com 22 carros, humano em destaque, contador de volta do líder, ticker de eventos, badge 🌧️, Acelerar ⏩ pula pro resultado), `TelaResultadoCorrida` (classificação FIA, DNF com volta, paradas, volta mais rápida, posição do humano em destaque), integração via `FluxoCorrida` + botão "Ir pra corrida" na TelaResumo. Revisado pelo senior-reviewer (aprovado com correções, ambas aplicadas: rAF fora do updater — bloqueante de StrictMode em dev — e composição `fracaoVisual`→`pontoNoTracado` com testes da composição).

- **PR 2.1a** — Generalização do draft pra N humanos (Fase 2; Single intacto). Engine: só `Jogador.nome?: string` (exibição; nunca entra em `deriveSeed` — ids fixos `voce`/`humano-1..4` garantem reprodutibilidade, comentário no código). UI: `fluxo-draft.ts` com `HumanoConfig`, `iniciarDraft`/`aplicarEscolhaDoJogador` genéricos (bots = 22 − nHumanos) + guardas de input (ids únicos/não-vazios, 1..22 humanos); `iniciarDraftSingle`/`aplicarEscolhaHumano` viram wrappers; `useDraft.escolher(jogadorId, escolha)`; telas destacam por `tipo === 'humano'` + `nomeJogador` (fallback 'Você', trim de nome vazio); TelaResumo com seção de carro por humano. Não-regressão do Single garantida por **golden snapshot capturado da main pré-PR** (worktree + junction de node_modules; `ordemPeca` completa, perfis de 3 bots e 5 sorteios do humano pra seed 'demo'/dificil — passou sem divergência) + testes de equivalência wrapper×genérico (4 casos, facil e dificil). Revisado pelo senior-reviewer (aprovado com correções — golden anti-tautologia, guardas de input e trim aplicados).

- **PR 2.1b** — Modo Local hotseat (turnos por bloco, GDD §2/§3). `src/ui/fluxo-local.ts` puro: `alvoHumano` (rodadas 1-5 em bloco por humano na ordem de cadastro — inócuo pro resultado, sorteios são sub-streams independentes por jogador; rodada 6 turno a turno seguindo `ordemPeca` da engine) e `decisaoLocal` (handoff/jogar/concluido dado quem confirmou estar com o aparelho — `confirmadoId` vive só no `App`, engine não sabe de handoff). Anti-vazamento: `TelaHandoff` neutra (só nome + fase, zero dado de jogo); `TelaDraft`/`TelaPeca` escopadas ao jogador ativo; `useDraft` limpa erro obsoleto no handoff. `TelaInicio` com modo Single/Local e 2-4 nomes; Single pula handoff (confirmado fixo). 12 testes novos em `fluxo-local.test.ts` (handoff inicial, blocos com 2/3/4 humanos, salto pra peça, sem handoff redundante, turno a turno na peça, concluído, teste estrutural anti-vazamento). Engine intocada. Revisado pelo senior-reviewer (aprovado sem bloqueantes nem avisos).

- **PR 2.3** — Modo Craque/Cego (GDD §5; fecha a Fase 2). Módulo puro `src/ui/visibilidade.ts`: tipo `Visibilidade = 'craque' | 'cego'`, `pecaVisivel(peca, visibilidade)` (view onde as chaves de dica — raridade, categoria, atributos-alvo, bônus, risco — literalmente NÃO EXISTEM no cego, não `undefined`, pra não vazar em spread/`Object.keys` futuro) e `mostrarNotas`. `CardPeca` virou burro (renderiza só chaves presentes — no cego perde até a borda colorida de raridade); `CardComponente` esconde `TabelaNotas` (cobre notas e base→efetiva da TelaResumo); categoria escondida no cego porque "Gambiarras"/"Roubos & Polêmicas" denunciam raridade. Opção da partida (não por jogador) na TelaInicio, válida pra Single e Local; `App` guarda e propaga. Telas de corrida/resultado verificadas: não exibem nota/raridade (não tocadas). 6 testes anti-tautológicos (`'chave' in view`, peças reais comum e proibida, pureza). Engine intocada. Revisado pelo senior-reviewer (aprovado; cosmético registrado: defesa do `CardComponente` é por não-render, mais fraca que a ausência-de-chave do `CardPeca` — nota de manutenção).

- **PR 2.4** — Seed automática (Fase 2.5, UX; engine intocada). TelaInicio não pede mais seed: sem seed específica, sorteia uint32 via `crypto.getRandomValues` na UI (nunca `Math.random`; corrige o caso "seed vazia ⇒ `seedFromString('')` ⇒ todo mundo sem seed jogava a mesma partida"). Campo opcional recolhido "🎲 Usar seed específica" (`<details>`; se recolhido ou vazio, sorteia — mesmo com texto digitado, intencional). Decisão pura em `fluxo-draft.ts`: `seedAleatoriaTexto(uint32)` (round-trip `seedDeTexto(seedAleatoriaTexto(x)) === x>>>0`) e `seedEfetivaTexto(usarEspecifica, texto, sortearUint32)` (sorteio injetado; não consumido no ramo específico — extraída da TelaInicio por aviso da revisão, testada sem DOM). TelaResultadoCorrida mostra "Seed da partida: {state.seed}" (número já resolvido — colar de volta cai no ramo `Number`, reproduz a partida mesmo se a original foi texto livre) + botão copiar (clipboard, feedback "Copiado!" 2s). 9 testes novos. Revisado pelo senior-reviewer (aprovado; aviso de cobertura da decisão inline resolvido com a extração de `seedEfetivaTexto`).

- **PR 2.5** — Seletor de pista (Fase 2.5, UX; engine intocada). `prepararCorrida(dataset, draftState, pistaId = PISTA_CORRIDA_ID)` — default preserva Monza bit a bit (nenhum teste de ouro alterado; hunk de teste 100% adição). Select "Pista" na TelaInicio com as 10 do dataset ordenadas por nome + linha de perfil via `perfilPista` puro (ultrapassagem 🟢🟡🔴, desgaste em bucket <40 Baixo / 40-69 Médio / ≥70 Alto — bate exato com o GDD §9 e os valores 25/50/75 do dataset —, chuva em %, voltas); perfil NÃO é ocultado no Modo Cego (informação pública da pista, não nota). `pistaId` flui App → FluxoCorrida → useCorrida → prepararCorrida, não entra em `deriveSeed` (mesma seed + mesma pista ⇒ mesmo resultado). `TRACADO_MONZA` renomeado `TRACADO_GENERICO` (traçado visual único e ilustrativo nesta fase; traçado por pista é Fase 4). 11 testes novos com dataset real. Revisado pelo senior-reviewer (aprovado sem bloqueantes; cosmético: fallback defensivo do select é código morto inofensivo).

- **PR 2.6** — Velocidades do replay + classificação ao vivo (fecha a Fase 2.5; engine intocada). `VelocidadeReplay` lenta/media/rapida = 9000/4500/2200 ms de relógio por volta (rápida = valor único anterior; default média ⇒ replay 2× mais lento que antes por padrão, recalibração pedida pelo dev); `escalaReplay` ganhou 3º parâmetro `msPorVolta` (default média). Troca DURANTE o replay: `useCorrida` lê o fator de `fatorEscalaRef` a cada frame do rAF (sem closure obsoleto; agendamentos fora de updaters — cuidado StrictMode preservado); trocar não reinicia nem pula tempo; Acelerar ⏩ intacto. `classificacaoAoVivo(resultado, gridLargada, tempoSimMs, voltasTotais)` pura — contrato: progresso desc; empate ambos-terminaram → tempoTotal asc (chegada real); demais empates → posição de largada (em t=0 reproduz o grid exato; DNFs afundam). Painel ao lado do SVG: 22 posições, nome, badge DNF, 🏁, humanos destacados; 3 botões 🐢▶️🐇 com `aria-pressed`. 9 testes novos com corrida real (seed com DNF em Suzuka), incluindo o teste do desempate entre terminados em t-após-todos-cruzarem (aviso da revisão, aplicado) e campo `volta` morto removido da view (sugestão da revisão). Revisado pelo senior-reviewer (aprovado sem bloqueantes).

- **PR 2.7** — `voltasDePit` na engine + status "no pit" no painel ao vivo (PR de engine autorizado pelo dev em 2026-07-21; fecha a pendência do PR 2.6). Engine: `ResultadoCorrida.voltasDePit: Record<string, number[]>` (voltas 1-based, estritamente crescentes, `length === paradas`), registro puro no mesmo ponto do `paradas++` de `simularCarro` — ZERO chamadas novas de RNG, mesmo padrão do `historicoVoltas` do PR 1.7b; seeds de ouro bit a bit (testes de ouro só desestruturam o campo novo; asserções antigas intactas) e relatório do `npm run balance` **idêntico byte a byte** pré/pós (verificado empiricamente pelo revisor via git stash). UI: `ItemClassificacaoAoVivo` ganhou status `'pit'` — só quando o carro estaria 'correndo' e a `voltaAtual` coincide com uma volta de parada ('terminou'/'dnf' têm precedência; NÃO reordena o sort; badge 🔧 PIT dura a volta inteira — aproximação documentada, o custo do pit é embutido no tempo da volta). TDD vermelho→verde na engine e na UI; teste extra com 22 carros do dataset inteiro. Revisado pelo senior-reviewer (aprovado sem bloqueantes nem avisos).

- **PR 2.8** — Traçados por pista (UI pura; antecipa item de polimento da Fase 4). `src/ui/tracados.ts`: `TRACADOS_POR_PISTA` com silhueta própria pra cada uma das 10 pistas + `tracadoDaPista(pistaId)` com fallback `TRACADO_GENERICO` (que virou "Monza estilizada + fallback"; Monza reaproveita por referência). Silhuetas ORIGINAIS estilizadas de 12-22 pontos desenhadas em coordenadas próprias (nota jurídica GDD §14.2: geometria de circuito é fato, mapa oficial F1/FIA é obra protegida — nada foi decalcado), mesmas coordenadas viewBox 0 0 1000 600 e mesma estrutura `Ponto[]` fechada — `pontoNoTracado` intocado. Suzuka é um "8" real (lemniscate de Gerono discretizada, cruzamento exatamente no vértice duplicado do centro — auto-interseção só visual, documentada; o percurso por comprimento de arco segue a sequência). TelaCorrida usa `tracadoDaPista(pista.id)` no path E nos carros (mesma fonte, sem desalinhamento) + legenda visível com o nome da pista sob o SVG. Revisão apontou 2 auto-interseções ACIDENTAIS (Spa e Interlagos, retorno cruzando a saída perto da largada) — corrigidas por nudge de coordenadas e verificadas por script de interseção; a guarda virou teste permanente (`cruzamentosMidSegmento === []` pra cada pista — cruzamento em vértice compartilhado não conta, preservando o 8 de Suzuka). 64 testes novos iterando o dataset real. Revisado pelo senior-reviewer (aprovado; 2 avisos aplicados).

- **PR 4.1** — Fetch + cache da Jolpica-F1 (1º da trilha do dataset histórico; `src/` intocado). `scripts/fetch-f1-data.ts` + `npm run dataset:fetch` (runner Node 24 nativo, sem tsx): results 1950-2025 paginado (`limit=100`, offsets do envelope MRData) + pitstops 2011-2025 por round (rounds extraídos das próprias páginas de results, sem requisição de schedule). Disciplina de rede: cache resume-safe em `scripts/cache/jolpica/` (gitignored; arquivo válido NUNCA é refeito; inválido é deletado e refeito), escrita atômica tmp+rename, throttle 1 req/10s (só em fetch real; hit não espera), backoff 60s×3 pra 429/5xx/erro de rede/200-com-corpo-malformado (este último era aviso da revisão — interstitial da Cloudflare vira retry, não SyntaxError fatal), 4xx≠429 pula só o endpoint, User-Agent identificando o projeto, ordem de URLs determinística. 27 testes mockados (zero rede) no `npm test`; harness segue isolado. Smoke-test real: 1 requisição (1950/results p0), cache válido, segunda rodada = hit. `scripts/node-shims.d.ts` (sem `@types/node` — declarações mínimas; aviso da revisão registrado: escopo global vaza `process` pra src/, follow-up = tsconfig próprio pra scripts/). Revisado pelo senior-reviewer (aprovado; aviso do parse aplicado com teste).

- **PR 4.2** — Agregador de fatos (`scripts/status-map.ts` + `scripts/agregar-fatos.ts` + `npm run dataset:fatos`; `src/` intocado). Tabela explícita status→categoria (terminou/acidente-erro/mecanica-chassi/mecanica-motor/outro; desconhecido → 'outro' + coletado em `statusesNaoMapeados`, auditoria nunca silenciosa) e set `NAO_LARGOU` (DNQ/DNPQ/Withdrew/DNS — fora de TODAS as contagens; aviso da revisão aplicado com fixture; DSQ/Excluded contam como largada). Exclusão da Indy 500 1950-60 antes de contar etapas; corte de escopo ≥ceil(etapas/3) rounds + 2 pilotos com ≥2 largadas; titulares por largadas→points→driverId; grid "0" (pit-lane) excluído de mediaGrid/poles e tratado como fundo do grid no recount de posições ganhas (decisão em sessão, testada com baseline vermelho); recontagem só-entre-quem-terminou anula posição herdada por DNF alheio (provado em teste); pit 2011+ (mediana delta vs corrida, fração estouradas >1.5×, parse "21.867" e "M:SS.mmm"); determinismo byte a byte (cmpStr por code unit, sem localeCompare — anti-ICU; números a 4 casas). 52 testes novos com fixtures sintéticas em `scripts/fixtures/jolpica-mini/` (Indy, carro compartilhado, DNQ, multi-equipe). Revisado 2× pelo senior-reviewer (aprovado; aviso das não-largadas aplicado). **Fetch real executado: 609 requisições, 0 abortos, 15MB de cache; agregação real: 76 temporadas, 771 equipe/anos elegíveis, 1542 titulares** (`fatos-agregados.json` commitado; McLaren-88 grid médio 1.875/15 poles confere com a história). Hotfix pós-merge: imports com extensão `.ts` (Node ESM nativo resolve diferente do Vitest) + `allowImportingTsExtensions`. Pendência pro 4.3: completar o status-map com os 37 statuses observados no dataset real (297× "Lapped" etc.).

- **PR 4.3** — Derivação de notas em staging (`scripts/derivar-notas.ts` + `npm run dataset:notas` → `scripts/derived/equipe-anos.derivado.json`, 771 entradas; `npm run dataset:report`; `src/` intocado — swap é o 4.5). Status-map completado com os 37 statuses reais (`Lapped`→terminou etc.; `statusesNaoMapeados` agora vazio nas 76 temporadas). Fórmulas do plano com decisões fechadas em sessão: shrink empírico-Bayes em TODAS as estatísticas brutas de piloto (n=largadas), equipe só CONF/CONF_MOTOR; prior AGRUPADO ponderado por largadas (não média simples); percentil de Hazen por temporada com rank médio em empates; faixas [28,96] geral / [35,80] CALL / [42,58] pit pré-2011 como config declarada. Testes com valores calculados à mão + invariantes sobre o derivado real (loader `criarDataset` aceita as 771; ids únicos; 76 temporadas; determinismo por hash). Revisado pelo senior-reviewer com RECOMPUTAÇÃO INDEPENDENTE (refez Hazen/shrink/CONF-1954 à mão: Mercedes 88/Ferrari 71/Maserati 54/Gordini 37 idênticos ao derivado; sinais conferidos um a um; grep anti-§14.1 vazio) — aprovado; cosmético do comentário de sinal do CALL aplicado. Spot-checks reais: Senna QUALI 95 (88-91), McLaren-88 AERO 93, Ferrari-2004 93, Minardi-95 45, Verstappen-23 RIT 92, RB-2012 PIT 93. **Achado pra decisão do dev: ULT premia recuperação — Verstappen-23 ULT 67 vs Pérez 94 (métrica correta pro que mede; candidata a override curado futuro, mesmo balde do CHU).** Próximo: PR 4.4 (desacoplamento byte a byte) — dev decide após ver os números.

- **PR 4.4** — Desacoplamento dos goldens do dataset vivo (só testes + fixture; `src/` de produção intocado). `src/fixtures/dataset-semente/` congela cópias byte-idênticas (sha256 conferido na revisão) de `equipe-anos.json`/`pecas.json`/`pistas.json` + README (nunca atualizar a fixture quando o vivo mudar); os 10 testes golden/conteúdo-específico (`engine/{bots,carro,corrida,dataset,draft,quali}` + `ui/{fluxo-draft-golden,fluxo-corrida,fluxo-local,useDraft}`) reapontados pra fixture com diff SÓ de import (nenhum valor esperado alterado); `tracados.test.ts`, `balance.balance.test.ts` e `dataset-app.ts` seguem no vivo de propósito (invariantes do que é entregue + gate do 4.5). Novo `dataset-live.test.ts`: invariantes do dataset VIVO (criarDataset aceita, ids únicos, 2 titulares, notas 0-99 — os 3 últimos são guarda de regressão do loader, gate real é o "aceita sem lançar"). Portões: 399 testes verdes; relatório do balance idêntico byte a byte pré/pós; **ensaio do swap** (derivado 771 por cima do vivo na working tree) passou 399/399 e foi restaurado. Revisado pelo senior-reviewer (aprovado; verificou hash das fixtures, grep de import residual vazio, zero produção alterada, fixture não vaza pro bundle).

- **PR 4.5** — Swap do dataset derivado + recalibração do sinal de grid (commits `8d2493a`+`777cad8`+`6c44f6d`). `src/data/equipe-anos.json` trocado do semente (22) pelo derivado 1950-2025 (771 entradas, hash idêntico ao staging do 4.3); suíte principal imune graças ao 4.4 (goldens na fixture). Harness adaptado pra resolver loadouts do dataset VIVO: `loadoutForte(dataset)` acha Red Bull/2023 e usa `pilotos[0]` (Verstappen, blindado por teste), e a lista fixa `PILOTOS_PARADAS` virou `selecionarPilotosParadas(dataset)` — min e max de PNEU por bucket (<60/60-80/>80), desempate por id em code unit (anti-ICU), throw em bucket vazio; PNEU lido do dataset (sem duplicação manual). Com o dataset novo o harness ficou vermelho em 3 metas (pole Monza 60%/Spa 64%; paradas alto 61.4%) — esse vermelho foi o baseline da recalibração (regra do CLAUDE.md). Decisões do dev (2026-07-22): **metas 1-2 recalibradas só via `CORRIDA_CONFIG.gridOffsetMs` 500/800/1200→800/1600/2100** (variancia 0.004 intocada; varredura empírica com 400 seeds mirando as 3 bandas simultâneas) ⇒ pole facil 67.8% / media 74.0% / dificil 81.5%, todas com folga. **Meta 3 NÃO recalibrou engine: a banda do agregado `paradas.alto` foi re-baseada [40,60]%→[40,70]% porque a BASE DA MEDIÇÃO mudou** (amostra hardcoded 1 baixo/3 médio/2 alto de PNEU → dinâmica 2/2/2 com extremos min/max, faixa 30-94; dobro de pilotos de PNEU baixo puxa o agregado pra cima sem nada ter piorado) — **o alvo do GDD não mudou**, e a intenção real ("desgaste alto força paradas variando pelo PNEU") ganhou guardas mais fortes por bucket: `pneuBaixo ≥ 75%` (medido 100%), `pneuAlto ≤ 40%` (medido 15.5%), contraste estrito mantido. Guardas de medição novas em `scripts/balance.test.ts` (no `npm test`): 6 pilotos pinados (regenerar dataset exige re-pinar conscientemente), throw em bucket vazio, Verstappen como titular nº1. Goldens de `corrida.test.ts` recongelados — revisão provou que SÓ `tempoTotal` deslocou (deltas lineares por posição de grid; ordem/pontos/paradas idênticos, pole intocado). Ratio da peça proibida 1.41 (teto 3.0). Revisado 2× pelo senior-reviewer (swap+harness dinâmico com recomputação independente dos 6 selecionados e prova de independência de ordem; recalibração+goldens aprovados sem achados).

- **PR 4.6** — Buckets de circuito: AERO/MEC/MOTOR diferenciados por fatos (fecha a exigência do D3 — os pesos por pista do GDD §9 agora discriminam de verdade). `scripts/circuit-buckets.ts`: tabela CURADA dos 77 circuitos do cache em potencia(18)/travado(16)/aero(23)/neutro(20) com âncoras do dev (Monza/Spa=potência, Mônaco/Hungaroring=travado, Silverstone/Suzuka=aero), casos de era mista documentados (Nürburgring→neutro; Hockenheim→potência pela era dominante 1970-2001), desconhecido→neutro+`circuitosNaoMapeados` no meta (vazio; auditoria nunca silenciosa). Agregador (aditivo, campos antigos byte-idênticos): `gridPercentilGeral` e `porBucket.{largadas,gridPercentil}` — percentil de Hazen `(N−pos+0.5)/N` por corrida entre largadores, grid "0" excluído, mesmo escopo do 4.2. Derivação com config declarada: `deltaShrunk = (percentilBucket − percentilGeral)·n/(n+8)`, `ajuste = clamp(delta·100, ±8)` (ESCALA=100 escolhida pela distribuição real: p95 pré-clamp 6.30 ∈ [5,8], documentada), `aero/mec/motor = clamp(base+ajuste, [28,96])`; bucket vazio ⇒ 0. Notas de piloto/estrategista/pit/ppeso/freio/conf/confMotor INTOCADAS (verificado programaticamente); pins do harness passaram sem edição. Divergência da base em ~83% das entradas; média dos ajustes ≈ 0; clamps 2.1%. Spot-checks: Red Bull 2011 aero 94>motor 93 ✔; McLaren-Honda 2015 motor 32 vs aero 44 ✔; Mercedes 2015 aero 94>motor 93 ✘ (limitação conhecida do proxy de grid — captura domínio geral, não potência isolada; registrado). **Balance: TODAS as bandas verdes sem recalibração** (pole 67.8/74.0/81.5 inalterado; paradas idem; proibida 1.40). Revisado pelo senior-reviewer com recomputação independente do cache cru (RB-2011 e Ferrari-1955 batendo 100%; reimplementação do ajuste sobre as 771 com 0 divergências; bijeção 77/77 da tabela; regeneração byte-idêntica) — aprovado; 1 aviso registrado (ver acompanhamentos).

- **PR 4.6.1** — Percentil de grid corrigido por dense-rank (fecha o aviso da revisão do 4.6). `percentilGridDaLinha` (`scripts/agregar-fatos.ts`) trocou o grid cru de qualificação por dense-rank ENTRE os largadores reais com grid>0 da prova (campo completo; duplicatas de grid — anomalia de dado — recebem o mesmo rank), N = contagem dessas linhas, `(N−rank+0.5)/N` ∈ (0,1) POR CONSTRUÇÃO (rank ≤ distintos ≤ N); grid "0" segue excluído (null) e contando em `largadas`. Em prova com campo contíguo 1..N sem grid-0, dense-rank(grid)=grid ⇒ valor idêntico ao anterior. TDD vermelho (fixtures sintéticas 1951/1952/1954: DNS de qualificados com grid>N, grid-0 no meio, grids duplicados; circuitIds reais neutros pra não sujar `circuitosNaoMapeados`) + 3 valores existentes recalculados à mão. Regeneração: fatos com SÓ `gridPercentil`/`gridPercentilGeral` tocados (grep verificado; 0 valores fora de (0,1) nos 3050 não-null), 578/771 `gridPercentilGeral` deslocam; notas: **373/2313 AERO/MEC/MOTOR mudam, 346 por ±1, máx 7** — topo é exatamente o GP dos EUA de 2005 (Indy, 6 largadores: Jordan aero 45→38/motor 30→37, Minardi aero 39→33, Ferrari motor 66→71) e o resto concentra em 1950-80, como previsto. Balance dentro das bandas SEM recalibração (só lendário ratio 0.91→0.97 e proibida 1.40→1.39 movem). Revisado pelo senior-reviewer com recomputação independente (fixtures refeitas à mão, reexecução do pipeline byte-idêntica, prova matemática da invariante) — aprovado sem bloqueantes nem avisos; cosmético pré-existente registrado (guard de grid NaN).

- **PR 4.7** — Overrides curados de ULT e CHU (fecha a trilha do dataset; decisões D4 + achado do 4.3). **CURADORIA EXPLÍCITA do dev, NÃO derivação de fatos** — esses dois atributos têm origem diferente do resto do dataset, documentado no cabeçalho de `scripts/overrides-curados.ts` e nos comentários de honestidade de `derivar-notas.ts`. `ULT_OVERRIDES` (31 ultrapassadores reconhecidos, tiers 96/90/84: Senna/Schumacher/Verstappen/Hamilton/Alonso/Mansell no 96) substitui o valor derivado — a métrica de remontada media recuperação, não ataque (Verstappen-23 saía 67 vs Pérez 94; agora 96 vs 84), e ULT alimenta o modelo de bloqueio que o Modo Cego depende. `CHU_OVERRIDES` (30 especialistas de chuva, tiers 96/88/80: Senna/Schumacher/Hamilton/Verstappen/Ickx/Moss/Stewart/Clark no 96) substitui o 50 neutro. Override vale pra CARREIRA INTEIRA do piloto; LARG segue 50 constante. Armadilhas de id resolvidas e testadas (`hill`=Graham, `damon_hill`=Damon, `rodriguez`=Pedro, `fittipaldi`=Christian ⇒ lista usa `emerson_fittipaldi`, `rosberg`=Nico ⇒ Keke é `keke_rosberg`). Guardas anti-typo/anti-morto: todo id existe entre titulares REAIS do `fatos-agregados.json` (JSON real, não mock), contagens exatas 31/30, faixa [28,96]. TDD vermelho (Verstappen-23 ult 67→96 / chu 50→96; Senna carreira inteira; não-listado preservado). Regeneração só via `dataset:notas` (fatos intocados): SÓ chaves `ult` (310 pilotos-ano) e `chu` (294) mudam; swap byte-idêntico. **Balance: pole e paradas IDÊNTICOS ao baseline (não dependem de ULT/CHU — escopo confirmado); guarda da proibida 1.43 ≤ 3.0 verde. Informativo pro dev aceitar conscientemente: o mix report-only da Meta 3/4 deslocou como efeito downstream esperado (elite com ULT/CHU reforçados muda quem vence campeonatos): comum 0.27→0.09, raro 0.14→0.05, épico 0.47→0.68, lendário 0.97→0.81, proibido 1.39→1.43.** Revisado pelo senior-reviewer (listas conferidas programaticamente contra o plano 31/31+30/30, spot-checks recomputados — Verstappen 11/11, Senna 10/10, Graham 16 vs Damon 7, Prost preservado —, balance rodado independentemente) — aprovado sem bloqueantes nem avisos.

- **PR 5.1a** — Fundação do design system arcade (1ª das 3 fatias do PR 5.1, Fase 5; engine intocada, zero dependência npm). Direção de arte ARCADE/LÚDICO, base escura "neon arcade" (decisões do dev). **`src/ui/tokens.ts`** é a fonte da verdade: paleta hex (fundo escuro `#16132E` + acentos vibrantes — amarelo `primaria`, ciano `acento`, magenta do humano, verde/azul/roxo/amarelo/vermelho de raridade), `PARES_CONTRASTE` (17 pares: texto ≥4.5:1, UI ≥3:1), escala de espaço (4-32), raios (8/12/16/pill), `alvoToque: 44`, breakpoints (480/768/1024); `borda` decorativa documentada fora dos pares AA (delimitação vem do contraste dos fundos). **`tokens.css`** espelha tudo como custom properties + `@font-face` Bungee + `--fonte-display`/`--fonte-corpo`. **Contraste como critério verificável** (`tokens.test.ts`): fórmula WCAG 2.x implementada à mão (limiar 0.03928, pesos 0.2126/0.7152/0.0722, caso preto/branco=21), assert de cada par ≥ mínimo, e sincronia `tokens.ts`↔`tokens.css` por regex+fs cobrindo cores **e** tokens px (aviso da revisão aplicado — divergência silenciosa de espaço/raio quebra o teste). **Guard-rail do Modo Cego** (`card-peca-cego.test.ts`): renderiza o `CardPeca` REAL via `renderToStaticMarkup` (react-dom/server, sem jsdom) com `pecaVisivel(peca, 'cego')` pra uma peça proibida ☠️ e uma comum 🟢 e exige HTML idêntico após normalizar só o nome + ausência de `raridade`/emojis/`Bônus`/`Risco`/`Alvo`/categoria — **nasceu VERDE** (sem vazamento atual; quebra na hora se 5.1b/c introduzir estilo derivado de raridade). Regra de design: card de peça é neutro por padrão ("card mistério"), raridade é modificador aditivo. `estilos.css`: `--cor-*` antigas repassam os tokens novos sem hex duplicado no `:root`; carro do humano → `--magenta` (papel dedicado, aviso da revisão), badges → `var(--texto-escuro)`; **sem redesenho de seletor** (raios/espaçamentos/estrutura ficam pra 5.1b/c). Fonte display **Bungee** (SIL OFL 1.1, confirmada; licença em `public/fonts/OFL-Bungee.txt`) self-hosted (`bungee-latin.woff2`, 14 KB) só em h1/h2; preload + `theme-color` no `index.html`; corpo em system stack. Balance-harness NÃO rodado (zero toque em nota/lógica/simulação — justificado). Revisado pelo senior-reviewer (APROVADO; recomputação independente da fórmula WCAG em 3 pares, confirmação de que o guard-rail é anti-tautológico de verdade — usa componente e view reais; 2 avisos aplicados: carro→magenta e sincronia px; 2 cosméticos registrados). **Visual híbrido esperado entre as fatias** (tokens novos sobre layout cru até 5.1b/c). Dev pediu parada aqui pra revisar a fundação antes das telas.

- **PR 5.1b** — Restyle arcade flat dos componentes base + telas de draft (2ª das 3 fatias do PR 5.1; engine/`visibilidade.ts`/hooks/`fluxo-*`/`loadout-view.ts`/`componentes.tsx` intocados; zero dependência). Aplica o estilo arcade/sticker consumindo os tokens da 5.1a em `TelaInicio`, `TelaDraft`, `TelaPeca`, `TelaResumo`, `TelaHandoff` + componentes base. **Botões**: flat "sticker" (cor chapada, borda 3px, `box-shadow` sólido sem blur), `min-height: var(--alvo-toque)` 44px (antes `.botao-secundario` ~33px), `:active` encosta na sombra, `:focus-visible` com outline; sombra/borda usam `--borda` (visível sobre `--fundo`; usar `--fundo` daria sombra invisível — corrigido durante a implementação). **Cards**: superfície elevada, raio generoso, sombra dura; **raridade é modificador aditivo só de `border-color`** (sombra neutra) — o senior-reviewer AUDITOU O CSS À MÃO e descartou vazamento do Modo Cego por CSS (que o guard-rail de `renderToStaticMarkup` não pegaria por não carregar CSS): no cego nenhuma classe `.raridade-*` é emitida (`pecaVisivel` não entrega a chave) e não há regra de card revelando raridade/categoria/bônus/risco; card cego = `.card` puro ("card mistério"). **Inputs/selects**: fundo afundado, borda interativa, 44px, foco visível. **Paleta 100% tokenizada**: quitados os débitos de hex do 5.1a — `.erro` (`#3a1414`→`var(--fundo-afundado)`) e `.linha-humano` (rgba fora da paleta → `border-left var(--magenta)`, papel dedicado do humano; senior-reviewer votou MANTER, reverter reintroduziria hex fora da paleta). **Mobile 360px**: grids `repeat(auto-fit, minmax(min(220px,100%),1fr))` colapsam pra 1 coluna; tabela do resumo em `.tabela-grid-wrap` (`overflow-x` no wrapper, não no body; `min-width:480px`); `flex-wrap` nos grupos de botão. `TelaResumo.tsx`: único .tsx tocado, só wrap da tabela em `<div>` (`git diff -w` = 2 linhas, zero lógica). Avisos da revisão aplicados: `min-height:44px` no `<summary>` da seed específica; 3 pares sobre `fundoAfundado` adicionados a `PARES_CONTRASTE` (trava por teste em vez de confiar em monotonicidade — texto 17.4:1, erro 7.7:1, borda 4.3:1); comentário obsoleto do `.erro` corrigido. Revisado pelo senior-reviewer (APROVADO com ressalvas, nenhum bloqueante; `TelaResumo` confirmado como só-wrap via `git diff -w`, guard-rails 56/56). **Visual híbrido segue** até a 5.1c (corrida/resultado ainda cruas).

- **PR 5.1c** — Restyle arcade das telas de CORRIDA e RESULTADO (3ª e ÚLTIMA fatia do PR 5.1; fecha o design system arcade). Engine/hooks/`fluxo-*`/`tracados.ts`/`visibilidade.ts`/`componentes.tsx` intocados; zero dependência. `.tracado-svg` e `.classificacao-ao-vivo` viram "cards" (borda 3px + `box-shadow: 4px 4px 0 var(--borda)` + `--raio-lg`) coerentes com a 5.1b. `.botao-velocidade`: sticker (borda/sombra 3px, `:active` afunda), `min-height: var(--alvo-toque)` 44px (antes ~30px), `:focus-visible`; `--ativo` usa `var(--primaria)` + `var(--texto-escuro)` (11.9:1, legível — cenário texto-claro-sobre-amarelo descartado pela revisão). Badges (chuva/DNF/PIT): raios/paddings via `var(--raio-pill)`/`--espaco-*`. **SVG só cor/espessura, geometria de `tracados.ts` intocada** (redesenho de traçado é 5.2); carro do humano segue `var(--magenta)`. **Destaque do humano**: friso magenta na classificação ao vivo (o item é `<li>`, e o friso genérico do `.linha-humano` só pegava `td:first-child` de tabela — lacuna real preenchida) e `.resultado-corrida__destaque` ciano→`var(--magenta)` (papel dedicado do jogador humano); ambos ratificados pela revisão (magenta/fundoElevado 5.08:1, magenta/fundo 5.91:1, indicador redundante com `font-weight:700`). **Mobile 360px**: `.tela-corrida__area-replay { flex-wrap:wrap }` com `min-width:0` nas 2 colunas (crítico — o `<svg>` é elemento substituído e sem isso força overflow horizontal), classificação `max-width: min(320px,100%)`, `grupo-velocidade` com `flex-wrap`, tabelas em `.tabela-grid-wrap`. `TelaCorrida.tsx`/`TelaResultadoCorrida.tsx`: único .tsx tocados, só wrap de tabela em `<div>` (`git diff -w` = 2 linhas cada, zero lógica). Cosmético da revisão aplicado: pares dos badges (`textoEscuro/erro` 7.2:1, `textoEscuro/raridadeLendario` 11.7:1) adicionados a `PARES_CONTRASTE`. Revisado pelo senior-reviewer (APROVADO; sem bloqueantes nem avisos; escopo reconfirmado por `git diff -w`, 3 decisões de cor ratificadas com contraste calculado). **Registrado pra limpeza futura (não bloqueante):** os bridge vars `--cor-destaque`/`--cor-destaque-clara`/`--cor-superficie-alta` no `:root` de `estilos.css` ficaram órfãos (sem consumidor `var()` em `src/`) após a tokenização das 3 fatias — remoção é mudança de fundação, candidata a PR de limpeza.

- **PR 6.1** — Engine de campeonato promovida do balance-harness (1º da Fase 6 — Modo Campeonato; plano do fable-architect aprovado pelo dev em 2026-07-25). `src/engine/campeonato.ts` (novo) recebe o que `scripts/balance.ts` reimplementava localmente desde o PR 1.6 (linhas 369-408): laço de etapas, derivação de seed por pista e acumulação de pontos. API: `seedDaEtapa`, `simularEtapa`, `acumularClassificacao`, `simularCampeonato`. **Rótulo do `deriveSeed` mantido LITERALMENTE `camp:${pistaId}`** — `deriveSeed` hasheia a string inteira, então renomear o prefixo mudaria todas as seeds e destruiria em silêncio o baseline calibrado nos PRs 1.6/4.5/4.6.1/4.7 (regra comentada no código-fonte, não só no PR). `simularEtapa` recebe a seed **já derivada** e nunca deriva internamente (decisão D6 do plano): a corrida avulsa segue chamando com a seed crua do draft, inalterada bit a bit — é o que permite campeonato e corrida rápida compartilharem código sem que as seeds já anotadas pelo dev mudem de resultado. **NÃO promovidos:** `draftarLoadoutsCampeonato` (setup de medição, 22 bots em dificuldade fixa) e as métricas do harness; o tipo local `ResultadoCampeonato` do harness virou `MetricasCampeonato` pra não colidir com o da engine. **Critério de aceite cumprido: relatório do `npm run balance` byte a byte idêntico ao da main** (verificado por `diff` e, independentemente, pelo revisor rodando a main num `git worktree` separado). `corrida.ts`/`quali.ts` intocados (nenhum golden congelado ameaçado); `types.ts` só aditivo; zero dependência nova. TDD vermelho antes da implementação e de novo antes de cada correção da revisão. Revisado pelo senior-reviewer (**aprovado sem bloqueantes**, com prova formal de que o desempate novo é equivalente ao laço antigo — a ordem (pontos desc, jogadorId asc) é total estrita porque os ids são únicos, logo máximo por varredura sequencial e `sort()[0]` coincidem e a estabilidade do sort é irrelevante). Achados aplicados: **A1** — vitórias/pódios/voltas rápidas só contam pra quem TERMINOU (`simularCorrida` põe finalizadores primeiro, mas com menos de 3 finalizadores um abandono cai em `posicao <= 3`; e em corrida 100% DNF a `voltaMaisRapida` aponta pra um abandonador sem creditar ponto, `corrida.ts:453-469`) — **corrigido no 6.1 de propósito, porque o desempate FIA do 6.2 consome esses contadores e nasceria contando vitória de quem abandonou**; **A2** — `simularCampeonato` rejeita `pistaId` duplicado (a seed depende só do id da pista, então repetir clonaria a corrida bit a bit e dobraria pontos em silêncio; incluir o índice no rótulo resolveria mas destruiria o baseline); **A3** — teste de desempate com ordem de entrada invertida (um comparador que devolvesse 0 no empate passava no teste antigo) + cobertura dos contadores novos, que estavam zerada; **S1** — `totalCarros` do harness deriva de `etapas.length`, não de `dataset.pistas.length`; **S2** — prova direta de que a seed vem do id e não do índice (etapas homônimas bit a bit iguais entre calendários permutados, em vez de só evidência agregada); **S4** — guard de loadouts vazio no padrão de `simularQuali`/`simularCorrida`. Próximo: PR 6.2 (desempate FIA oficial).

**Testes na main: 512 passando** (27 arquivos: 441 + 50 do 5.1a + 3 pares do 5.1b + 2 pares do 5.1c + 16 do 6.1) + 1 do harness via `npm run balance`. Lint, `tsc --noEmit` e `npm run build` limpos.

**🏁 PR 5.1 (design system arcade) CONCLUÍDO (2026-07-25):** as 3 fatias (5.1a tokens+fonte+guard-rails, 5.1b componentes+telas de draft, 5.1c telas de corrida/resultado) fecham a fundação visual da Fase 5. Todas as 7 telas restiladas no estilo arcade/flat "sticker" (fundo neon escuro, botões chapados com sombra dura, Bungee nos títulos). Paleta 100% tokenizada com contraste WCAG travado por teste (22 pares em `PARES_CONTRASTE`), alvos de toque 44px, mobile 360px por design. **Modo Cego preservado**: guard-rail de indistinguibilidade (`renderToStaticMarkup`) + auditoria manual do CSS descartaram vazamento de raridade (nenhuma classe `.raridade-*` emitida no cego; card cego = "card mistério" neutro). Engine intocada em todas as fatias. Próximo da Fase 5: PR 5.2 (traçados arcade das 10 pistas). Ordem geral pós-dataset ainda em aberto: dev decide entre continuar a Fase 5 (visual) e a Fase 3 (online/PartyKit). Nada pushado.

**🏁 Trilha do dataset (PR 4.x) CONCLUÍDA (2026-07-23):** dataset 1950-2025 com 771 equipe/anos derivado de fatos da Jolpica (percentil de grid saneado por dense-rank no 4.6.1), AERO/MEC/MOTOR diferenciados por buckets de circuito (4.6), e ULT/CHU com overrides de curadoria explícita (4.7). Todos os atributos derivados de fatos EXCETO ULT (override curado sobre derivado), CHU (override curado sobre 50) e LARG (50 constante). Próximo: decisão do dev entre Fase 3 (online) e Fase 5 (visual).

**🏁 Marco da Fase 1 atingido:** dá pra jogar o modo Single completo (`npm run dev`): draft de 6 rodadas contra 21 bots → quali → corrida animada → resultado FIA. Balance-harness operante. Visual segue propositalmente cru — polimento é Fase 4.

**🏁 Marco da Fase 2 atingido (2026-07-21):** jogável presencialmente com amigos — modo Local hotseat de 2-4 jogadores com handoff anti-vazamento, e Modo Craque/Cego pra Single e Local. Fase 3 (Online/PartyKit) é a próxima do roadmap; nada foi pushado.

## Acompanhamentos registrados pela revisão do PR 1.6 (não são defeitos; candidatos a PR futuro)

- `medirParadasExtras` usa equipes históricas inteiras — o CALL do estrategista desloca a 1ª parada e vira confound secundário do bucket de PNEU (o bucket <60 é na prática 1 piloto). Sinal mais limpo: fixar chassi/motor/estrategista/pit e variar só o piloto.
- ~~`medirRaridadePeca` usa draft uniforme simplificado~~ **Resolvido no PR 1.6.1** (pedido do dev em 2026-07-18): a métrica agora roda o motor de draft real (`criarDraft` + `resolverBots`, 22 bots em dificuldade 'dificil'). Resultado: ratio da Proibida subiu de 1.25 (uniforme) pra **1.51** — ainda ≤ 3.0, guarda verde. Números com draft real: playerShare proibido 59.0% (bots pra-ganhar preferem peça forte), championShare 89.0%. O ratio normalizado é saudável, mas o championShare absoluto alto é esperado dado o uso massivo; se o dev quiser reduzir a onipresença da Proibida, o knob é bônus/risco no JSON de peças (decisão explícita do dev, não recalibrar por conta).
- Margens finas e determinísticas nos asserts do harness: facil 64.3% vs piso 63%; alto 56.0% vs teto 60%. Mudança de dataset pode exigir recalibração — rodar `npm run balance` sempre que tocar notas/fórmulas.

## Metas de calibração (decididas pelo dev em 2026-07-18 — cumpridas no PR 1.6)

1. **Adiamento do harness confirmado** — PRs 1.3/1.4/1.5 entram com constantes-chute; o PR 1.6 calibra tudo. ✅
2. **Sinal de grid**: 61/100 é fraco demais. Meta: pole com carro idêntico vence **claramente mais que 61% e bem menos que 95%** (alvo ~70-80%). Direção: subir `gridOffsetMs` e/ou baixar `variancia`. O harness DEVE medir a taxa de vitória do pole e reportar se ainda está fraca. ✅ (facil 64.3%, media 72.5%, dificil 85.3%)
3. **Parada extra em desgaste Alto (75)**: 10% é baixo pro "força paradas extras" do GDD §9. Meta: a **maioria dos carros** (~40-60%, variando pelo PNEU do piloto) faz 2+ paradas em pista de desgaste Alto. Direção: baixar `limiarPneuGasto` ou subir a curva de degradação. ✅ (alto 56.0%; bucket PNEU<60 100%, 60-80 80.3%, >80 0%)
4. Harness também reporta: win-rate por raridade de peça (guarda contra peça dominante, GDD §14.3). ✅ (ratio proibido 1.25, limite 3.0)

## Próximos

- **Fase 6 — Modo Campeonato (EM ANDAMENTO; plano do fable-architect aprovado pelo dev em 2026-07-25).** Encadeia as 10 pistas do GDD §9 numa temporada com pontos FIA acumulados, tabela de classificação e campeão. Ponto de partida: o `balance.ts` já simulava campeonatos desde o PR 1.6 — o 6.1 promoveu essa lógica pra engine em vez de reescrevê-la.
  **Decisões aprovadas pelo dev (todas as 7, como propostas):** **D1** draft UMA vez por campeonato (re-draft destruiria a escassez de 2 cópias da peça e a trava do online, GDD §4/§7); meio-termo de trocar a peça entre corridas rejeitado na v1; se a medição do 6.3 mostrar dominância, a alavanca é um "pit de meio de temporada" (após a 5ª etapa, troca de UM componente), não re-draft. **D2** ritmo: assistir por default, com "Só o resultado" e "Simular o resto" — as 10 pistas somam **132 voltas** ⇒ ~9,9 min de replay puro no default de 4500ms/volta (PR 2.6), ~13 min com overhead de tela; rápida ~7,8 min, lenta ~22,8 min. **D3** pré-simular o campeonato inteiro quando o draft fecha (custo medido <2ms por campeonato — o harness faz 200 campeonatos + 2.100 corridas em 436ms), o que torna "Simular o resto" instantâneo e a persistência trivial. **D4** persistência em `localStorage` (`src/ui/persistencia.ts`, `Storage` injetado pra testar sem jsdom): salva SÓ ENTRADA (seed + `DraftState` + calendário + etapa), nunca os `ResultadoCorrida`; guardas = `versaoFormato` (shape) + **impressão digital auto-verificante** (hash dos pontos da etapa 1, recomputado ao retomar — invalida o save sozinho se dataset ou engine mudarem). `versaoEngine` manual foi rejeitada explicitamente: é constante que se esquece de bumpar, com falha silenciosa. **D5** calendário fixo na ordem do GDD §9, guardado como `string[]` (não hardcode) — habilita temporada curta e calendário custom sem mudar o formato de save. **D6** corrida rápida e campeonato COEXISTEM com código unificado mas **seeds separadas**: avulsa usa a seed crua (comportamento de hoje preservado bit a bit), campeonato usa `seedDaEtapa`; unificar a seed mudaria toda corrida avulsa sem quebrar nenhum teste — mudança silenciosa, rejeitada. **D7** hotseat: o handoff só existe no draft, então as 10 etapas são assistidas juntas sem handoff nenhum; mitigação do tempo total = seletor "Temporada curta (5 etapas)" (as 5 primeiras somam 68 voltas ⇒ ~5,1 min). Online: campeonato é função de `seed + loadouts`, exatamente o que o servidor já vai distribuir — **zero mudança na arquitetura de rede**. **D8** sub-streams: `seedEtapa = deriveSeed(seedCampeonato, 'camp:' + pista.id)`, por **id da pista, nunca por índice** ⇒ ordem do calendário e ordem de execução são irrelevantes; quali e corrida compartilham a seed da etapa sem colidir porque os rótulos internos diferem.
  **Sequência:** ~~6.1 promoção da engine~~ (concluído) → 6.2 desempate FIA oficial → **6.3 PORTÃO DE DECISÃO** (métrica "o draft decide o campeonato?" — correlação de Spearman entre força do loadout e posição final, report-only; **o dev pediu PARADA aqui pra ver os números antes de construir qualquer tela**) → 6.4 `fluxo-campeonato.ts` puro → 6.5 persistência → 6.6 telas (design system 5.1) → 6.7 TelaInicio (modo, temporada curta, retomar). Se precisar cortar escopo, cortar pelo 6.7; nunca pelo 6.3.

- **Dataset histórico 1950-2025 (trilha PR 4.x — EM ANDAMENTO, plano do fable-architect aprovado pelo dev em 2026-07-21).** Princípio anti-GDD §14.1: scripts derivam notas de FATOS da API Jolpica-F1 (`https://api.jolpi.ca/ergast/f1/`, Ergast-compatível, 500 req/h — throttle aprovado 1 req/10s, cache resume-safe rigoroso, NUNCA refetch); nada de nota "no olho". Pipeline em 4 estágios: `fetch-f1-data.ts` (cache cru gitignored) → `agregar-fatos.ts` (`scripts/derived/fatos-agregados.json` COMMITADO — auditabilidade) → `derivar-notas.ts` (percentil de Hazen por temporada + shrinkage + faixa-alvo [28,96], único knob livre) → `dataset-report.ts` (histogramas + spot-checks). Decisões do dev: escopo = equipe/ano com ≥1/3 das etapas e 2 pilotos com ≥2 largadas, Indy 500 1950-60 excluída (D1); QUALI = grid dos results uniforme 1950-2025 (D2); AERO/MEC/MOTOR iguais na v1 MAS **PR 4.6 (buckets de circuito) é OBRIGATÓRIO** — sem ele os pesos de pista do GDD §9 viram decoração e peça de AERO vs MOTOR dá no mesmo; dataset NÃO está pronto sem o 4.6 (D3); CHU = 50 neutro + **override curado de 30-50 especialistas de chuva** (curadoria explícita documentada, não derivação; LARG fica 50) (D4); runner = Node 24 nativo, sem tsx (D8, `node -v` = 24.16.0). Sequência: ~~4.1 fetch → 4.2 agregador → 4.3 derivação staging → 4.4 desacoplamento → 4.5 swap + balance → 4.6 buckets de circuito~~ (TODOS concluídos e mergeados) → ~~restavam CHU curado e decisão sobre override do ULT~~ **Resolvidos no PR 4.7** (overrides curados de ULT e CHU; trilha CONCLUÍDA em 2026-07-23 com o 4.6.1 + 4.7).

## Acompanhamentos registrados pela revisão do PR 4.6 (não bloqueantes)

- ~~Percentil de grid pode sair de [0,1] com dado real dos anos 50~~ **Resolvido no PR 4.6.1** (dense-rank entre largadores reais; (0,1) garantido por construção).
- Cosmético: o report imprime média dos ajustes −0.05 (base pré-clamp); medida sobre as notas finais inteiras é −0.038. Alinhar a base de medição no print numa próxima passada.
- **Fase 3 — Online (PartyKit)** — PENDENTE, começando pelo PR 3.1 (setup PartyKit, sala com código, entrar/sair, preencher com bots até 22). Exige plano do fable-architect + aprovação do dev antes de implementar (PLANO §5 Fase 3).
- **Fase 5 — Identidade visual e polimento (registrada em 2026-07-22; EXECUTAR depois do dataset).** Direção de arte ARCADE/LÚDICO: cores vibrantes, estilo chapado (flat), divertido — coerente com o espírito "7x1/38 a 0" e contorna a ausência de arte fotorrealista/mapas oficiais. Escopo em PRs (detalhado no PLANO §5 Fase 5): 5.1 design system (paleta, tipografia, tokens, componentes base flat — substitui o visual cru), 5.2 traçados arcade das 10 pistas (silhuetas PRÓPRIAS, nunca decalcar o mapa oficial F1/FIA — GDD §14.2), 5.3 editor de capacete (designs originais que evocam épocas, nunca cópia nomeada de pintura real), 5.4 animações/transições (draft, corrida, celebração), 5.5 tela de abertura + marca (nome/logo), 5.6 som (efeitos e talvez música; biblioteca leve, sem dependência pesada).
- **Ordem pós-dataset:** decidir entre Fase 3 (online) e Fase 5 (visual) — ambas grandes, escolha do dev.
- ~~Pendência do PR 2.6: status "parado no pit" exige engine expor a volta de cada parada~~ **Resolvido no PR 2.7** (autorizado pelo dev em 2026-07-21).

## Acompanhamentos registrados pela revisão do PR 1.7b (cosméticos, candidatos à Fase 4)

- `useCorrida`: o initializer do `useState` roda `prepararCorrida` 2× na montagem em dev (StrictMode) — determinístico e inofensivo, só CPU.
- Ticker de eventos da `TelaCorrida` usa a volta do líder como relógio comum — evento de retardatário pode aparecer "adiantado" em relação à posição dele no traçado.
- Replay com todos-DNF (improvável): o relógio do replay é o tempoTotal do 1º classificado, que num grid 100% DNF encurta o replay.
- ~~Pista da corrida é fixa (Monza); seletor de pista fica pra fase futura~~ **Resolvido nos PRs 2.5 (seletor) e 2.8 (traçado próprio por pista — item de Fase 4 antecipado a pedido do dev em 2026-07-21).**

## Convenções que os PRs seguem

- Branch `feat/pr-X.Y-nome` → commit `feat:` → `git merge --no-ff` na main com mensagem `merge: PR X.Y — ...`.
- TDD: teste vermelho antes da implementação; seed de ouro por módulo de simulação.
- RNG: sub-stream por jogador (`deriveSeed(seed, 'fase:${jogadorId}')`) pra independência de ordem.
- Fluxo: junior-dev implementa → testes/lint/tsc → senior-reviewer revisa → correções → commit local. **Push só com ok explícito do dev.**
